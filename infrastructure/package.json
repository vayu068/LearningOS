{
  "name": "@learning-os/infrastructure",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all",
    "lint": "eslint . --ext .ts --max-warnings 0",
    "test": "echo \"No tests yet\"",
    "clean": "rm -rf dist cdk.out .turbo node_modules"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.140.0",
    "constructs": "^10.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "aws-cdk": "^2.140.0",
    "typescript": "^5.4.0"
  }
}
